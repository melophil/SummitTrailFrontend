Summit Full Project (backend + admin) generated.

Backend: /mnt/data/summit_full/backend
Admin: /mnt/data/summit_full/admin

Instructions:
1. Open backend folder, copy .env.example to .env and update MONGO_URI and secrets.
2. Run `npm install` inside backend, then `npm run seed` then `npm run dev`.
3. Open admin folder, run `npm install` then `npm run dev`.
